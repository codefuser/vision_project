import type { RendererProps } from "./index";

/**
 * Song slide renderer — architectural placeholder. Replaced when the Songs
 * Module ships. Engine never routes `song_slide` here until then.
 */
export function SongRenderer(_props: RendererProps) {
  return null;
}
