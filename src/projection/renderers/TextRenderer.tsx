import type { RendererProps } from "./index";

/**
 * Live text renderer — architectural placeholder. Replaced when the Live
 * Text Module ships.
 */
export function TextRenderer(_props: RendererProps) {
  return null;
}
